import 'package:flutter/material.dart';

class FCMDEMO extends StatelessWidget {
  const FCMDEMO({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text("Okay Google!")),
    );
  }
}
